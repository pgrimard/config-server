server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/config
  application:
    name: configserver

encrypt:
  failOnError: false
  keyStore:
    location: file://${user.home}/server.jks
    alias: server
